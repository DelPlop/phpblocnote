{% extends '@DelPlopPbn/pbn.html.twig' %}
{% trans_default_domain 'notes' %}

{% block title %}
    {% if type =='add' %}{{ ('form.add'|trans({}, 'messages')) }}{% else %}{{ ('form.edit'|trans({}, 'messages')) }}{% endif%} {{ ('categories.a_cat'|trans) }}
{% endblock %}

{% block body %}
    {% block pageTitle %}
        <h1 class="w3-text-cyan">
            {% if type =='add' %}{{ ('form.add'|trans({}, 'messages')) }}{% else %}{{ ('form.edit'|trans({}, 'messages')) }}{% endif%} {{ ('categories.a_cat'|trans) }}
        </h1>
    {% endblock %}

    {{ form(form) }}
{% endblock %}
